apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: divesh-boutique
spec:
  selector:
    app: frontend
    #version: blue
    version: green
  ports:
  - port: 80
    targetPort: 8080